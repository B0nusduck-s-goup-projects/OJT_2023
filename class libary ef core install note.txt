-download apropriate version of efcore.tool/sqlserver/design on
 both start up and class libary project.

-manually type out context file, let it inherit dbcontext and
 override "OnConfiguring(DbContextOptionsBuilder builder)" and
 "OnModelCreating(ModelBuilder modelBuilder)".

-+add connection string by let option builder using:
 |
 +------"builder.UseSqlServer(@"connection string")"
 |
 |-----+or registering the dbcontext along with the connectionstring in the
       |program.cs file of the start up project using:
       |
       |--------"builder.Services.AddDbContext<DbContext>(option =>
		option.UseSqlServer(@"connection string"));"

-+connection string
 +-"Server=ADMIN\SQLEXPRESS;User ID=;Password=;Database=;MultipleActiveResultSets=False;TrustServerCertificate=True;"

-add dependencies to the projects (startup project->...->data class libary)

-+if the dbcontext hadn't been registered in the start up project via program.cs
 |then register it using:
 +-"builder.Services.AddDbContext<DbContext>();"

-add entity

-build and add migration + update database (check if everything work)

*note: the context constructor will display warning when a new entity is declared,
ignore it.